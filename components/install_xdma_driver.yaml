name: Install2
description: DummyInstall2
schemaVersion: 1.0
phases:
    - name: build
      steps:
        - name: print
          action: ExecuteBash
          inputs:
            commands:
              # TODO: Put updated XDMA device header file on S3, pull down, replace, then perform install
                - echo "Second"
                - sudo echo Second >> ~/test.txt
                - sudo cat ~/test.txt